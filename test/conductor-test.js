describe('Conductor Class', function() {
    var Conductor = require('../src/conductor');

    it ('should have loadPack defined as static method', function() {
        // This is a sample test to make sure test framework is up and running
        expect(Conductor.loadPack).toBeDefined();
    });
});
